import 'package:flutter/material.dart';

class StoreViews extends StatelessWidget {
  const StoreViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("stor"),);
  }
}
